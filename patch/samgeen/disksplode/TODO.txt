INITIAL CONDITIONS
- Debug rho_ana and condinit = DONE
- Add variables to namelist = DONE

STARS
- Add star particles by hand in ascii file (movement turned off in amr_step)
--> Needs calculating SFR, etc
- Set IDs to be -ve and age (tp) so that explosion time = -(current_time+10Myr)
- if(idp(ipart).le.0.and. tp(ipart).lt.(current_time-t0))then

COOLING
- Allow constant cooling functions
- Make sure cooling_module_ph is properly hacked
